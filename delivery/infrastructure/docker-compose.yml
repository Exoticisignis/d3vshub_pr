version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8080:8080
    depends_on:
      - db
  db:
    image: postgres:latest
    ports:
        - "5432:5432"
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"